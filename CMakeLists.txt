cmake_minimum_required(VERSION 3.5)
include($ENV{IDF_PATH}/tools/cmake/project.cmake)
# set VERSION to prevent a full rebuild whenever the git revision changes
project(avionics-software-2024-2025 VERSION 1)
